[Unit]
Description=SelfControl Daemon - Automatic website unblocking
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/selfcontrol-daemon
Restart=always
RestartSec=10
User=root

# Logging
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
